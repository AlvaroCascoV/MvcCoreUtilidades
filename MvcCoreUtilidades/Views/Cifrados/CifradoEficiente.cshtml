<h1 style="color:blue">Cifrado Eficiente con salt</h1>

<form method="post">
    <label>Contenido a cifrar: </label>
    <input type="text" name="contenido" class="form-control" />

    <label>Resultado cifrado: </label>
    <textarea rows="5" name="resultado" class="form-control">@ViewData["TEXTOCIFRADO"]</textarea>

    <button class="btn btn-primary" name="accion" value="cifrar">Cifrar Contenido</button>
    <button class="btn btn-secondary" name="accion" value="comparar">Comparar cifrado</button>
</form>

<h2 style="color:red">@ViewData["SALT"]</h2>
<h2 style="color:blue">@ViewData["MENSAJE"]</h2>